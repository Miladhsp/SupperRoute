name: supper_route
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
#required
environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  source_gen: '>=0.8.0'
  cupertino_icons: ^1.0.3

dev_dependencies:
  build_runner: ^2.0.4 # <-- And this line
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
